# Ревью и план рефакторинга package-lock.json

## 📊 Текущее состояние

### Размер и структура
- **Размер файла**: 7957 строк
- **Lockfile версия**: 3 (актуальная)
- **Статус**: Структура корректна, размер нормален для проекта с множеством зависимостей

### Анализ зависимостей

#### ✅ Используемые зависимости (frontend)
- `@dnd-kit/*` - используется в AdminPage, SortableAgentItem, AdminProjectTypesTab
- `lucide-react` - используется в App.tsx и других компонентах
- `react`, `react-dom` - основные зависимости
- `react-markdown`, `remark-gfm` - используется для рендеринга markdown
- `react-syntax-highlighter` - используется для подсветки синтаксиса
- `uuid` - **НЕ используется во frontend**, только в backend

#### ⚠️ Проблемы

1. **Уязвимости безопасности** (moderate):
   - `@vitest/coverage-v8@2.1.9` → требуется обновление до 4.0.15 (major)
   - `@vitest/ui@2.1.9` → требуется обновление до 4.0.15 (major)
   - `vitest@2.1.9` → требуется обновление до 4.0.15 (major)
   - `esbuild@<=0.24.2` (через vite) → уязвимость в dev-сервере

2. **Устаревшие пакеты**:
   - `@dnd-kit/modifiers@6.0.1` → доступна версия 9.0.0 (major)
   - `vite@6.4.1` → доступна версия 7.2.6 (major)
   - `tailwindcss@3.4.18` → доступна версия 4.1.17 (major)
   - `vitest@2.1.9` → доступна версия 4.0.15 (major)
   - `jsdom@25.0.1` → доступна версия 27.2.0 (major)
   - `typescript@5.8.3` → доступна версия 5.9.3 (patch)

3. **Неиспользуемые зависимости**:
   - `uuid@13.0.0` в корневом package.json - используется только в backend

4. **Дублирование типов**:
   - `@types/node@22.19.1` в корне
   - `@types/node@24.10.1` в backend
   - Разные версии могут вызывать конфликты

## 🎯 План рефакторинга

### Этап 1: Очистка зависимостей
1. Удалить `uuid` из корневого package.json (используется только в backend)
2. Проверить и удалить другие неиспользуемые зависимости

### Этап 2: Исправление уязвимостей (приоритет)
1. Обновить `vitest` и связанные пакеты до 4.x (major update)
2. Обновить `vite` до 7.x (major update) - это также обновит esbuild
3. Проверить breaking changes и обновить код при необходимости

### Этап 3: Обновление зависимостей (опционально)
1. Обновить `@dnd-kit/modifiers` до 9.x (major) - проверить совместимость
2. Обновить `tailwindcss` до 4.x (major) - требует миграции конфигурации
3. Обновить `jsdom` до 27.x (major)
4. Обновить `typescript` до 5.9.3 (patch)

### Этап 4: Оптимизация структуры
1. Пересоздать `package-lock.json` после всех изменений
2. Проверить размер и структуру нового lock файла
3. Убедиться, что все зависимости разрешены корректно

## ⚠️ Важные замечания

### Breaking Changes при обновлении

1. **Vitest 2.x → 4.x**:
   - Изменения в API конфигурации
   - Возможные изменения в тестах
   - Требуется обновление `@vitest/coverage-v8` и `@vitest/ui`

2. **Vite 6.x → 7.x**:
   - Изменения в конфигурации
   - Обновление esbuild (исправление уязвимости)
   - Возможные изменения в плагинах

3. **TailwindCSS 3.x → 4.x**:
   - **Крупное обновление** с breaking changes
   - Требуется миграция конфигурации
   - Рекомендуется отложить до отдельного этапа

4. **@dnd-kit/modifiers 6.x → 9.x**:
   - Проверить совместимость с текущими версиями core и sortable
   - Возможны изменения в API

## 📝 Рекомендации

### Немедленные действия (безопасные)
1. ✅ Удалить `uuid` из корневого package.json
2. ✅ Обновить `typescript` до 5.9.3 (patch)
3. ✅ Обновить `react` и `react-dom` до 19.2.1 (patch)

### Действия с тестированием (критичные для безопасности)
1. ⚠️ Обновить `vitest` до 4.x (исправить уязвимости)
2. ⚠️ Обновить `vite` до 7.x (исправить уязвимость esbuild)
3. ⚠️ Протестировать сборку и тесты после обновления

### Отложенные действия (major updates)
1. 📅 Обновить `tailwindcss` до 4.x (отдельный этап миграции)
2. 📅 Обновить `@dnd-kit/modifiers` до 9.x (проверить совместимость)
3. 📅 Обновить `jsdom` до 27.x (если не критично)

## 🔄 Процесс рефакторинга

### Шаг 1: Очистка
```bash
# Удалить uuid из корневого package.json
npm uninstall uuid
```

### Шаг 2: Обновление критичных пакетов
```bash
# Обновить vitest и связанные пакеты
npm install --save-dev vitest@latest @vitest/coverage-v8@latest @vitest/ui@latest

# Обновить vite
npm install --save-dev vite@latest

# Обновить typescript (patch)
npm install --save-dev typescript@latest

# Обновить react (patch)
npm install react@latest react-dom@latest
```

### Шаг 3: Пересоздание lock файла
```bash
# Удалить старый lock файл и node_modules
rm package-lock.json
rm -rf node_modules

# Переустановить зависимости
npm install
```

### Шаг 4: Проверка
```bash
# Проверить уязвимости
npm audit

# Проверить устаревшие пакеты
npm outdated

# Запустить тесты
npm test

# Проверить сборку
npm run build
```

## 📈 Ожидаемые результаты

После рефакторинга:
- ✅ Исправлены все уязвимости безопасности
- ✅ Удалены неиспользуемые зависимости
- ✅ Обновлены критичные пакеты
- ✅ Оптимизирован размер package-lock.json (возможно уменьшение на 5-10%)
- ✅ Улучшена производительность сборки и тестов

## ⚡ Альтернативный подход (консервативный)

Если major updates вызывают опасения:
1. Исправить только уязвимости (vitest, vite)
2. Удалить неиспользуемые зависимости
3. Обновить только patch версии
4. Отложить major updates до отдельного этапа миграции

