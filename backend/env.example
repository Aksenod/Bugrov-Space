# PostgreSQL Database URL
# Для локальной разработки используйте External Database URL (с полным доменом)
# Для продакшена на Render используйте Internal Database URL (без домена)
# Оба URL должны содержать ?sslmode=require для SSL подключения
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

JWT_SECRET="change_me_in_production"
OPENAI_API_KEY="your_server_side_openai_key"
CORS_ORIGIN="https://bugrov.space"

